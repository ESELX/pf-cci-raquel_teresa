  
  let button;
function setup() {
  createCanvas(400, 400);
  background(0);
  button = createButton('Botão da resposta!');
  button.position(49, 219);
  button.mousePressed(Resposta);
}

function Resposta() {
  let val = random(255);
  background(val);

    } 